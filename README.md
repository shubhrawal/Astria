# Astria
RCC Spring 26 Project


# LLM Service

Create a `.env` file in this folder with your Gemini API key:

GOOGLE_API_KEY=your_key_here

Then run `docker compose up --build` from the root folder.
